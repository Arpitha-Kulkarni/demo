---
 - name: playbook
   hosts: localhost
   become: yes
   tasks:
     - name: update apt cache
       apt:
         update_cache: yes
     - name: Install apache2
       apt:
         name: apache2
         state: present 
